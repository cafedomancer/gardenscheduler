<% provide(:title, '作業記録 | ') %>
<% provide(:description, '') %>

<h2>作業記録 / <%= "#{@task.schedule.variety.name}(#{@task.schedule.name})@#{@task.schedule.prefecture&.name}" %></h2>

<%= render 'form', task: @task %>
