<%= form_with(model: schedule) do |form| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
        <% schedule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label Schedule.human_attribute_name(:name) %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label Schedule.human_attribute_name(:variety_id) %>
    <%= form.collection_select :variety_id, Variety.all, :id, :name, options = { include_blank: true } %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
